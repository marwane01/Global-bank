<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.GlobalBank" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/non</item>
        <item name="colorPrimaryVariant">@color/non</item>
        <item name="colorOnPrimary">@color/non</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/non</item>
        <item name="colorSecondaryVariant">@color/non</item>
        <item name="colorOnSecondary">@color/non</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>
</resources>